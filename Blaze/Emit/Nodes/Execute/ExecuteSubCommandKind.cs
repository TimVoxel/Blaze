namespace Blaze.Emit.Nodes.Execute
{
    public enum ExecuteSubCommandKind
    {
        Align,
        Anchored,
        As,
        At,
        Facing,
        If,
        In,
        On,
        Positioned,
        PositionedAs,
        PositionedOver,
        Rotated,
        RotatedAs,
        StorePath,
        StoreScore,
        StoreBossbar,
        Unless,
    }
}
